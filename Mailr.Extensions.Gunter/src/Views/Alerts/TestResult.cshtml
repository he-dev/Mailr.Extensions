@model Mailr.Extensions.Gunter.Models.v1.Alerts.TestResultBody

<br />

@foreach (var module in Model.Modules.OrderBy(x => x.Value.Ordinal))
{
    Html.RenderPartial($"Modules/{module.Key}", module.Value);
}

<hr />

@{
    Html.RenderPartial("Footer");
    //Html.RenderPartial("~/src/Views/Shared/Footer.cshtml");
}

